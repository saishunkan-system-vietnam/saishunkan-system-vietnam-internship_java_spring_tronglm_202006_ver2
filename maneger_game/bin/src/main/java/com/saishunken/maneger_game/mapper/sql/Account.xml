<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.saishunken.maneger_game.mapper.MapperAccount">
	<insert id="insert" parameterType="Account">
		INSERT INTO account (name_account, password, mail, role_name, del_flg, phone_number, create_date, edit_date) 
		VALUES (#{name_account}, #{password}, #{mail}, #{role_name}, #{del_flg}, #{phone_number}, #{create_date}, #{edit_date});
	</insert>

	<update id="update" parameterType="Account">
		UPDATE account SET	
		<if test="password != null">
			password = #{password},
		</if>
		
		<if test="phone_number != null">
			phone_number = #{phone_number},
		</if>
		
		<if test="del_flg != null">
			del_flg = #{del_flg},
		</if>
		
		<if test="role_name != null">
			role_name = #{role_name},
		</if>
		
		<if test="mail != null">
			mail = #{mail},
		</if>
		edit_date = #{edit_date}
		WHERE id = #{id};
	</update>

	<select id="getById" parameterType="int"
		resultType="Account">
		SELECT * FROM account WHERE id = #{id};
	</select>

	<select id="getByEmail" parameterType="String"
		resultType="Account">
		SELECT * FROM account WHERE mail = #{mail};
	</select>
	
	<select id="getByName" parameterType="String"
		resultType="Account">
		SELECT * FROM account WHERE name_account = #{name_account};
	</select>
	
	<select id="getTotalByName" resultType="int">
		SELECT Count(*) FROM account WHERE name_account = #{name_account}
		<if test ="id != null">
			and id != #{id};
		</if>
	</select>
	
	<select id="getTotalByPhone" resultType="int">
		SELECT Count(*) FROM account WHERE phone_number = #{phone_number}
		<if test ="id != null">
			and id != #{id}
		</if>
	</select>
	
	<select id="getTotalByMail"	resultType="int">
		SELECT Count(*) FROM account WHERE mail = #{mail}
		<if test ="id != null">
			and id != #{id}
		</if>
	</select>

	<select id="getAll" parameterType="Account"
		resultType="Account">
		SELECT * FROM account WHERE del_flg = 0
		<if test="name_account != null">
			and name_account LIKE '%${name_account}%'
		</if>
		<if test="role_name != null">
			and role_name = #{role_name}
		</if>
		LIMIT  #{maxPageItem} 
		OFFSET #{offset} 	
	</select>

	<select id="getTotalUser" parameterType="Account" resultType="BigInteger">
		SELECT Count(*) FROM account WHERE del_flg = 0
		<if test="name_account != null">
			and name_account LIKE '%${name_account}%'
		</if>
		<if test="role_name != null">
			and role_name = #{role_name}
		</if>
	</select>
</mapper>