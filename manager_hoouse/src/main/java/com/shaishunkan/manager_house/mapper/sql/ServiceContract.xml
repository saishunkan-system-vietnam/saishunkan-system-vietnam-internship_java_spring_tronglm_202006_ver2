<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shaishunkan.manager_house.mapper.MapperServiceContract">
	
	<insert id="insert" parameterType="ServiceContract" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
		INSERT INTO service_contract (id_service, id_contract, price, del_flg, create_date, update_date, amount)
		VALUES (#{id_service}, #{id_contract}, #{price}, #{del_flg}, #{create_date}, #{update_date}, #{amount})
	</insert>
	
	<update id="update" parameterType="ServiceContract" >
		UPDATE service_contract SET
		<if test="price != null">
			price = #{price},
		</if>		
		<if test="del_flg != null">
			del_flg = #{del_flg},
		</if>			
		<if test="amount != null">
			amount = #{amount},
		</if>
		update_date = #{update_date}
		WHERE id = #{id};
	</update>
	
</mapper>